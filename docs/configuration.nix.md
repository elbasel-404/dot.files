# configuration.nix

Main NixOS configuration file. Imports modules from the `modules/` directory and sets system-wide options, including hardware, users, services, and desktop environments. This file is the entry point for building the system configuration.
